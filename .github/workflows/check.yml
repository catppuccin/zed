name: whiskers check
on:
  workflow_dispatch:
  pull_request:

jobs:
  run:
    uses: catppuccin/whiskers/.github/workflows/check.yml@ci/reusable-action
    with:
      templates: zed.tera
    secrets: inherit
